% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st_stage_table.R
\name{st_stage_table}
\alias{st_stage_table}
\title{st_stage_table(x, table_name)}
\usage{
st_stage_table(
  x,
  table_name = deparse(substitute(x)),
  db_name = "Elmer",
  schema_name = "stg",
  geom_type = "geometry",
  srid = 2285,
  geometry_column = "Shape",
  layer_name = NULL
)
}
\arguments{
\item{x}{An \code{sf} object, an \code{sfc} geometry vector, or a path to a GeoPackage
(\code{.gpkg}) file.}

\item{table_name}{String. The name you want given the database table.}

\item{db_name}{String. The name of the database to stage into. Defaults to
"Elmer".}

\item{schema_name}{String. The target schema. Defaults to "stg".}

\item{geom_type}{String. The SQL Server spatial type to create: "geometry"
or "geography". Defaults to "geometry".}

\item{srid}{Integer. The spatial reference id to assign to the geometry
column. Defaults to \code{2285}, which is the standard projection for most
SQL Server spatial tables in Elmer and ElmerGeo. Input data are
reprojected to this SRID before writing.}

\item{geometry_column}{String. The name of the geometry column to create.
Defaults to \code{"Shape"}, which is the standard geometry column name on
this SQL Server.}

\item{layer_name}{String. If \code{x} is a GeoPackage path, optionally select a
specific layer from that file.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Write a simple-features object as a SQL Server spatial table.
}
\details{
This is the spatial analogue to \code{\link[=stage_table]{stage_table()}}. It writes the non-spatial
attributes with \code{\link[DBI:dbWriteTable]{DBI::dbWriteTable()}} and then converts the geometry to a
native SQL Server \code{geometry} or \code{geography} column.
}
\note{
This creates a native SQL Server spatial table. It does not register
the output as an ESRI geodatabase feature class.
}
